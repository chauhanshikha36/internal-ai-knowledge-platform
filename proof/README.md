Put the proof of execution here:

1. `python scripts/demo.py --with-delete | tee proof/demo_output.txt` (run against `docker compose up`)
2. Screenshots or a short screen recording of the run, plus the `/docs` page.
